pipeline {
    agent any
    stages {
        stage ('deliverJsonPayload') {
            steps{
                echo "${RootSchemaAzr}"
            }
        }
    }
    post {
        success {
            echo "Test succeeded"
        }
        failure {
            echo "Test failed"
        }
    }
}