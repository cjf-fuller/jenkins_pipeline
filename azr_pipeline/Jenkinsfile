pipeline {
    stages {
        stage ('deliverJsonPayload') {
            echo "${RootSchemaAzr}"
        }
    }
    post {
        success {
            echo "Test succeeded"
        }
        failure {
            echo "Test failed"
        }
    }
}