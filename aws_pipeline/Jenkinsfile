pipeline {
    agent any
    stages {
        stage ('deliverJsonPayload') {
            echo "${RootSchemaAws}"
        }
    }
    post {
        success {
            echo "Test succeeded"
        }
        failure {
            echo "Test failed"
        }
    }
}