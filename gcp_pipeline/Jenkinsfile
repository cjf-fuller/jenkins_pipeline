pipeline {
    agent any
    stages {
        stage ('deliverJsonPayload') {
            steps{
                echo "${env.RootSchemaGcp}"
            }
        }
    }
    post {
        success {
            echo "Test succeeded"
        }
        failure {
            echo "Test failed"
        }
    }
}