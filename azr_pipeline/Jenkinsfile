pipeline {
    agent any
    stages {
        stage ('deliverJsonPayload') {
            steps{
                echo "${buildVariables.RootSchemaAzr}"
            }
        }
    }
    post {
        success {
            echo "Test succeeded"
        }
        failure {
            echo "Test failed"
        }
    }
}