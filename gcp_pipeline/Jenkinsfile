pipeline {
    agent any
    stages {
        stage ('deliverJsonPayload') {
            steps{
                echo "${buildVariables.RootSchemaGcp}"
            }
        }
    }
    post {
        success {
            echo "Test succeeded"
        }
        failure {
            echo "Test failed"
        }
    }
}